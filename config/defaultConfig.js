const config = {
    // 启动端口
    port: 3001,

    mysqlConfig: {
        database: "zhusen",
        user: "root",
        password: "adminzhusen",
        port: "3306",
        host: "localhost"
    }
};

module.exports = config;
